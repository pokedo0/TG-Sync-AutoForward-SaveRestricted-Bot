我需要实现tg bot的项目，功能：
- 普通bot + userBot 结合实现
- 私聊bot 群组，频道对话链接（私人或公开），可以返回受限访问的链接内容
- 指定同步(本质还是复制转发)，源：某群组(某话题 如果开启了)/频道 到 指定 某群组或频道，同步之前的所有内容，即完成该任务。
- 指定监控转发，源：某群组(某话题 如果开启了)/频道 到 指定 某群组或频道，监控源头之后新增内容，同步转发（可指定保留forward, 还是去掉署名的 copy 一份），该功能实时开启监控。
- 单纯通过tg的指令交互，需要考虑交互便携逻辑
- 以上所有功能，尽量基于防止被tg 官方封禁某些频道 群组内容而导致之前转发内容看不到，才需要做这个项目的目的考虑。

以上功能需要详细考虑到 
- 私有群组/频道如何访问转发，
- 如果源开启防止转发保护，如何突破。
- 可以指定 群组下话题对话题 颗粒度的 同步与转发。
- 考虑源频道的消息本身有可能也是转发的消息
- 如果源消息是来自于某个消息下的评论区内容，该评论区消息是否可以顺利转发，比如https://t.me/zaihuapd/40254?comment=8341963
- 如果遇到无法转发复制的内容，代替发出源频道无法转发的消息链接+ #fail2forward 标记

技术上需要的考虑：
- bot可以添加到私人频道,群组中，以求最小成本转发消息，比如可以转发，肯定比先下载再上传的方式好，需要结合判断bot自身环境最适合的最小成本转发逻辑。你需要告诉我，如果转发两个私人群组间的内容，bot是否需要分别加入到他们之中是最低成本实现。
- 技术上，需要考虑tg原生api是否符合我的功能需求，forwardmessage 还是 copymessage
- python 技术栈的选择，tg原生api还是 telethon,aiogram,Pyrogram,python-telegram-bot , 详细对比，还是有其他更合适的 框架等 合适我？以日常更新最新，star数多优先， 需要考虑大规模同步转发消息避免封号的场景。 
- 如果必须是Userbot，考虑我同步之前大量内容，是否会触发限流等，如何解决，网上是否有相关案例 ，公开的群组内容是否就不会涉及限流？
- userBot的话，怎么设置转发可以减少封号风险
- bot 对视频转发大小是否有限制
- 需要关注限流封号等，触发 FloodWaitError 异常处理，详细查找正确的降流处理 分批获取等
- 尽量通过api bot直接转发操作，减少userbot操作 减少封号风险
- 交互上，是否我可以只添加bot到目标群组，然后指令添加源频道进行转发？而不需要加入源频道，考虑交互设计

给出上面的需求方案，技术简单方案等。
有不清楚的地方 先问我清楚后 再执行